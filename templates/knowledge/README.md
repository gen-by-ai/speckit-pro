# .knowledge/ bootstrap templates

Copied into `<project-root>/.knowledge/` by `/speckit.pro.knowledge-sync --mode bootstrap`.

- **Commit** files at `.knowledge/` root (`INDEX.md`, `domain/`, …).
- **Do not commit** `.knowledge/features/` or `.knowledge/metrics/` — SpecKit Pro gitignores them in Phase 5b.
